"""Automation, setup, validation, and demo scripts for the skill.

This package exists so the console entry points declared in ``pyproject.toml``
(``hcra-demo``, ``hcra-validate``) resolve correctly after installation.
"""
